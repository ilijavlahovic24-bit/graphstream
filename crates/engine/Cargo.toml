[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
temporal_graph = { path = "../temporal_graph" }
query = { path = "../query" }
tokio = { version = "1", features = ["rt", "macros", "net"] }
tracing = "0.1"
anyhow = "1"